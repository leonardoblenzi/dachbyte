# Volt Core Neon baseline — 2026-08-17

Captured before deploying Sprint 1.

- Neon project: `volt core` (`fancy-unit-77003307`)
- Branch: `production` (`br-weathered-feather-a6k18j6x`)
- PostgreSQL: 17
- Plan: Launch
- Capture time: `2026-08-17T18:43:01.974Z`

## Neon project counters for the current consumption period

At capture time the project reported:

- compute time: `260356 s` (~72.32 CU-compute-hours at the raw endpoint counter level; billing CU-hours depend on actual CU allocation/autoscaling);
- active time: `549860 s` (~152.74 hours);
- data transfer: `7134099802 bytes` (~6.64 GiB);
- logical branch size: `37634048 bytes` (~35.89 MiB);
- endpoint minimum compute: `0.25 CU`;
- endpoint maximum compute: `8 CU`.

Do not treat `active_time_seconds` as CU-hours. Use Neon consumption/billing metrics for billed CU-hours.

## PostgreSQL database counters

These are cumulative counters. Compare deltas from a later snapshot; do not compare raw totals alone.

| Metric | Baseline |
|---|---:|
| xact_commit | 23,284,385 |
| xact_rollback | 105 |
| temp_files | 0 |
| temp_bytes | 0 |
| deadlocks | 0 |
| tup_inserted | 8,025 |
| tup_updated | 2,170 |
| tup_deleted | 122 |
| numbackends at capture | 2 |

## Primary polling indicators

| Table | Live rows | Seq scans | Seq tuples read | Index scans |
|---|---:|---:|---:|---:|
| `volt_core.integration_outbox_events` | 35 | 6,806,383 | 176,574,381 | 6,799,363 |
| `volt_core.integration_jobs` | 0 | 192,724 | 50 | 128 |
| `volt_core.sales` | 42 | 1,109,941 | 41,958,678 | 363 |
| `volt_core.receivables` | 16 | 1,100,570 | 16,505,853 | 6,934 |
| `volt_core.customers` | 57 | 64,276 | 3,324,185 | 56,080 |

The high scan count on tiny relations is primarily a frequency signal; it is not, by itself, evidence that an index is missing.

## Sprint 1 comparison

After a controlled deploy, capture the same counters over comparable idle/business windows and calculate deltas per hour. Primary success signals:

1. sharp reduction in `xact_commit` delta during idle periods;
2. sharp reduction in `integration_outbox_events` and `integration_jobs` scan deltas;
3. no increase in failed/retried integration jobs;
4. no loss of wake latency for locally committed jobs/outbox events;
5. observable Neon suspend windows when no other Volt Core workload is active.
