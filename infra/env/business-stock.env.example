# Copy to business-stock.env and fill with STAGING-only values.
# Private PostgreSQL URLs use host postgres and sslmode=disable.
# Create separate databases/roles and restore schemas before starting apps.
VOLT_STOCK_DATABASE_URL=
VOLT_STOCK_JWT_SECRET=
VOLT_STOCK_JWT_REFRESH_SECRET=
VOLT_STOCK_QR_HMAC_SECRET=
