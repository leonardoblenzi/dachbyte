# Volt Stock Mobile

Espaco reservado para o APK React Native/Expo, voltado a:

- leitura por camera;
- bipagem produto/local;
- inventario e transferencia offline-first;
- fila local de sincronizacao e resolucao de conflitos.

A implementacao deve consumir a mesma API versionada de `apps/api`.
