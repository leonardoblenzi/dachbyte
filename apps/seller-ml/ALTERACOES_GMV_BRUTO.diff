--- a/services/financeiroMlService.js
+++ b/services/financeiroMlService.js
@@ -65,11 +65,6 @@
   return Number.isFinite(n) ? n : null;
 }
 
-function paymentIsUsable(payment = {}) {
-  const status = normalizeString(payment?.status).toLowerCase();
-  return status === "approved";
-}
-
 function orderLifecycleStatus(order = {}, shipment = {}) {
   const orderStatus = normalizeString(order?.status).toLowerCase();
   const shippingStatus = normalizeString(
@@ -118,73 +113,41 @@
   return "Preparando";
 }
 
-function readPaymentTotalAmount(payment = {}) {
-  const netReceivedAmount = [
-    payment?.transaction_details?.net_received_amount,
-    payment?.net_received_amount,
-  ]
-    .map(maybeNumber)
-    .find((amount) => amount != null && amount >= 0);
-  if (netReceivedAmount != null) return netReceivedAmount;
-
-  const totalPaidAmount = maybeNumber(
-    payment?.total_paid_amount ?? payment?.transaction_details?.total_paid_amount,
-  );
-  const marketplaceFee = maybeNumber(payment?.marketplace_fee);
-  if (totalPaidAmount == null || totalPaidAmount < 0 || marketplaceFee == null || marketplaceFee < 0) {
-    return null;
-  }
-
-  const amount = totalPaidAmount - marketplaceFee;
-  return amount >= 0 ? amount : null;
-}
-
-function resolveOrderPaymentGmv(order = {}) {
-  const usablePayments = Array.isArray(order?.payments)
-    ? order.payments.filter(paymentIsUsable)
-    : [];
-  const paymentIds = usablePayments.map((payment) => String(payment?.id));
-  const amounts = usablePayments.map(readPaymentTotalAmount);
-
-  if (!usablePayments.length || amounts.some((amount) => amount == null)) {
-    return { amount: null, source: "unavailable", payment_ids: paymentIds };
-  }
-
-  return {
-    amount: amounts.reduce((total, amount) => total + amount, 0),
-    source: "order.payments.total",
-    payment_ids: paymentIds,
-  };
-}
-
 function resolveRealizedGmv({
-  paymentGmv = null,
+  orderProductTotal = null,
   fullOrderRevenue = 0,
   revenueBase = 0,
   buyerShippingFull = 0,
-  fullOrderCommissions = 0,
-  sellerCouponDiscountFull = 0,
   selectionRatio = 1,
   totalCosts = 0,
 } = {}) {
-  const paymentAmount = maybeNumber(paymentGmv?.amount ?? paymentGmv);
-  const hasPaymentAmount = paymentAmount != null && paymentAmount >= 0;
-  const fullGmv = hasPaymentAmount
-    ? paymentAmount
-    : Math.max(
-      0,
-      (numberOrZero(fullOrderRevenue) || numberOrZero(revenueBase)) +
-        numberOrZero(buyerShippingFull) -
-        numberOrZero(fullOrderCommissions) -
-        numberOrZero(sellerCouponDiscountFull),
-    );
-  const gmv = fullGmv * Math.max(0, Math.min(1, numberOrZero(selectionRatio)));
-  const profit = gmv - numberOrZero(totalCosts);
+  // REGRA DAVANTTI: GMV e o TOTAL BRUTO DO PEDIDO, antes das deducoes
+  // financeiras do Mercado Livre. Na order do ML, `total_amount` representa
+  // os produtos; o frete pago pelo comprador vem do shipment e compoe o total
+  // bruto exibido pelo integrador.
+  //
+  // Exemplo:
+  //   produtos 734,44 + frete comprador 96,71 = GMV 831,15
+  //
+  // Comissao, cupom, imposto, CMV e tarifa de envio continuam como custos
+  // separados e sao descontados somente depois, no Resultado.
+  const explicitProductTotal = maybeNumber(orderProductTotal);
+  const productTotal =
+    explicitProductTotal != null && explicitProductTotal >= 0
+      ? explicitProductTotal
+      : numberOrZero(fullOrderRevenue) || numberOrZero(revenueBase);
+  const fullGmv = Math.max(0, productTotal + numberOrZero(buyerShippingFull));
+  const ratio = Math.max(0, Math.min(1, numberOrZero(selectionRatio)));
+  const gmv = roundNumber(fullGmv * ratio, 2);
+  const profit = roundNumber(gmv - numberOrZero(totalCosts), 2);
 
   return {
     gmv,
-    source: hasPaymentAmount ? paymentGmv?.source || "order.payments.total" : "order.derived.total",
-    payment_ids: Array.isArray(paymentGmv?.payment_ids) ? paymentGmv.payment_ids : [],
+    source:
+      explicitProductTotal != null && explicitProductTotal >= 0
+        ? "order.total_amount+buyer_shipping"
+        : "order.items+buyer_shipping",
+    payment_ids: [],
     product_revenue: numberOrZero(revenueBase),
     profit,
     marginPct: gmv > 0 ? (profit / gmv) * 100 : 0,
@@ -2730,7 +2693,7 @@
           : 1;
         const totalUnits = items.reduce((sum, row) => sum + Math.max(1, numberOrZero(row.quantity)), 0);
         const shipmentId = pickShipmentId(order);
-        const [shipment, discounts, paymentGmv] = await Promise.all([
+        const [shipment, discounts] = await Promise.all([
           fetchShipmentCosts(state, shipmentId, seller.id).catch(() => ({
             buyer_shipping: numberOrZero(
               pickNested(order, ["shipping.cost", "shipping.cost_to_buyer", "shipping.base_cost"]),
@@ -2745,21 +2708,23 @@
             summary.coupons_available = false;
             return { seller_coupon_discount: 0, meli_rebate: 0, embedded_seller_discount: 0 };
           }),
-          Promise.resolve(resolveOrderPaymentGmv(order)),
         ]);
         // Valores de pedido/shipment sao de nivel da order. Quando a consulta esta
         // filtrando apenas parte dos itens, alocamos proporcionalmente para nao
         // atribuir o frete/cupom inteiro a um subconjunto da venda.
         const sellerCouponDiscountFull = numberOrZero(discounts.seller_coupon_discount);
-        const coupon = sellerCouponDiscountFull * selectionRatio;
+        const coupon = roundNumber(sellerCouponDiscountFull * selectionRatio, 2);
         // Rebate/estorno do ML e informativo: order_items[].sale_fee ja representa
         // a tarifa efetivamente cobrada (liquida). Nao somar novamente no resultado.
-        const rebate = numberOrZero(discounts.meli_rebate) * selectionRatio;
-        const embeddedSellerDiscount = numberOrZero(discounts.embedded_seller_discount) * selectionRatio;
+        const rebate = roundNumber(numberOrZero(discounts.meli_rebate) * selectionRatio, 2);
+        const embeddedSellerDiscount = roundNumber(
+          numberOrZero(discounts.embedded_seller_discount) * selectionRatio,
+          2,
+        );
         const buyerShippingFull = numberOrZero(shipment.buyer_shipping);
         const shippingTariffFull = numberOrZero(shipment.seller_cost);
-        const buyerShipping = buyerShippingFull * selectionRatio;
-        const shippingTariff = shippingTariffFull * selectionRatio;
+        const buyerShipping = roundNumber(buyerShippingFull * selectionRatio, 2);
+        const shippingTariff = roundNumber(shippingTariffFull * selectionRatio, 2);
         const collectionFee = isColetasLogistic(shipment.logistic_type)
           ? shippingTariff
           : 0;
@@ -2804,32 +2769,37 @@
             has_cost: Boolean(sku && unitCost > 0),
           };
         });
-        const productCost = resolvedItems.reduce((sum, row) => sum + numberOrZero(row.cost_total), 0);
-        const commissions = resolvedItems.reduce((sum, row) => sum + numberOrZero(row.sale_fee), 0);
-        const fullOrderCommissions = (Array.isArray(rawAllItems) ? rawAllItems : items)
-          .reduce((sum, row) => sum + numberOrZero(row.sale_fee), 0);
-
-        // REGRA FISCAL DA DAVANTTI (2026-08): GMV continua sendo somente o valor
-        // dos produtos. A base do imposto, porem, reflete o total tributavel do
-        // pedido: total_amount da order + frete efetivamente pago pelo comprador.
-        // receiver.cost, em /shipments/{id}/costs, e a fonte do frete do comprador.
-        // Em consultas filtradas por item, a base e alocada proporcionalmente.
+        const productCost = roundNumber(
+          resolvedItems.reduce((sum, row) => sum + numberOrZero(row.cost_total), 0),
+          2,
+        );
+        const commissions = roundNumber(
+          resolvedItems.reduce((sum, row) => sum + numberOrZero(row.sale_fee), 0),
+          2,
+        );
+
+        // GMV e Base imposto partem do total bruto do pedido: produtos +
+        // frete pago pelo comprador. `order.total_amount` e a fonte primaria
+        // para o valor dos produtos; quando ausente, usamos a soma dos itens.
+        // Em consultas filtradas por item, o total e alocado proporcionalmente.
         const orderProductTotal = numberOrZero(order?.total_amount) || fullOrderRevenue || revenueBase;
-        const fullTaxBase = Math.max(0, orderProductTotal + buyerShippingFull);
-        const taxBase = fullTaxBase * selectionRatio;
-        const taxes = taxRate > 0 ? taxBase * taxRate : 0;
+        const fullGrossOrderGmv = Math.max(0, orderProductTotal + buyerShippingFull);
+        const fullTaxBase = fullGrossOrderGmv;
+        const taxBase = roundNumber(fullTaxBase * selectionRatio, 2);
+        const taxes = taxRate > 0 ? roundNumber(taxBase * taxRate, 2) : 0;
 
         // unit_price ja e o preco promocional. Somente cupom do vendedor aplicado
         // no checkout e uma deducao adicional a receita exibida em unit_price.
         const sellerCheckoutDiscounts = coupon;
-        const totalCosts = productCost + commissions + taxes + shippingTariff + sellerCheckoutDiscounts;
+        const totalCosts = roundNumber(
+          productCost + commissions + taxes + shippingTariff + sellerCheckoutDiscounts,
+          2,
+        );
         const realizedGmv = resolveRealizedGmv({
-          paymentGmv,
+          orderProductTotal,
           fullOrderRevenue,
           revenueBase,
           buyerShippingFull,
-          fullOrderCommissions,
-          sellerCouponDiscountFull,
           selectionRatio,
           totalCosts,
         });
@@ -2839,8 +2809,9 @@
           ? Math.max(0, Math.min(0.95, commissions / revenueBase))
           : 0;
         const variableRate = commissionRate + taxRate;
-        // O frete pago pelo comprador aumenta a base fiscal sem aumentar o GMV.
-        // Sua parcela tributaria entra como custo fixo no equilibrio realizado.
+        // O frete pago pelo comprador compoe o GMV bruto e a base fiscal.
+        // Para o equilibrio do preco do produto, apenas a parcela tributaria
+        // desse frete entra como custo fixo adicional.
         const taxBaseExtra = Math.max(0, taxBase - revenueBase);
         const fixedBreakEvenCosts = productCost + shippingTariff + sellerCheckoutDiscounts + (taxBaseExtra * taxRate);
         const breakEvenTotal = variableRate < 0.99
@@ -3603,8 +3574,6 @@
       MODO_ENVIO: row.shipping_mode || "",
       TIPO_LOGISTICO: row.logistic_type || "",
       GMV: numberOrZero(row.gmv),
-      FONTE_GMV: row.gmv_source || "",
-      IDS_PAGAMENTO: (Array.isArray(row.payment_ids) ? row.payment_ids : []).join(" | "),
       PRECO_PRODUTO: numberOrZero(row.product_revenue),
       CMV: numberOrZero(row.product_cost),
       TARIFAS_MARKETPLACE_LIQUIDAS: numberOrZero(row.commissions),
@@ -3629,13 +3598,13 @@
   const sheet = XLSX.utils.json_to_sheet(rows, {
     header: [
       "PEDIDO", "DATA", "MLBS", "SKUS", "VARIACOES", "PRODUTOS", "QUANTIDADE_TOTAL", "MODO_ENVIO",
-      "TIPO_LOGISTICO", "GMV", "FONTE_GMV", "IDS_PAGAMENTO", "PRECO_PRODUTO", "CMV", "TARIFAS_MARKETPLACE_LIQUIDAS",
+      "TIPO_LOGISTICO", "GMV", "PRECO_PRODUTO", "CMV", "TARIFAS_MARKETPLACE_LIQUIDAS",
       "COMISSAO_EFETIVA_PERCENTUAL", "FRETE_COMPRADOR", "BASE_IMPOSTO", "IMPOSTOS", "IMPOSTO_PERCENTUAL", "TARIFA_ENVIO",
       "CUPOM_VENDEDOR", "REBATE_ML_INFORMATIVO", "CUSTOS_TOTAIS", "RESULTADO",
       "MARGEM_PERCENTUAL", "EQUILIBRIO_UNITARIO", "FOLGA_UNITARIA", "CUSTO_COMPLETO", "ITENS_SEM_CUSTO", "STATUS",
     ],
   });
-  setSheetWidths(sheet, [18, 12, 28, 28, 22, 60, 16, 18, 20, 15, 28, 24, 20, 15, 20, 22, 18, 18, 15, 18, 16, 18, 24, 16, 18, 16, 18, 20, 18, 16, 16]);
+  setSheetWidths(sheet, [18, 12, 28, 28, 22, 60, 16, 18, 20, 15, 20, 15, 20, 22, 18, 18, 15, 18, 16, 18, 24, 16, 18, 16, 18, 20, 18, 16, 16]);
   XLSX.utils.book_append_sheet(workbook, sheet, "Margem por periodo");
   return workbook;
 }
@@ -4277,7 +4246,6 @@
 }
 
 FinanceiroMlService._test = {
-  resolveOrderPaymentGmv,
   resolveRealizedGmv,
   orderLifecycleStatus,
   buildOrderSearchQuery,
--- a/views/financeiro-ml-margem.html
+++ b/views/financeiro-ml-margem.html
@@ -140,7 +140,7 @@
             <span>GMV considerado</span>
             <div class="fml-metric-value">
               <strong id="fml-summary-gmv">R$ 0,00</strong>
-              <span class="fml-help" tabindex="0" aria-label="GMV usa preferencialmente o valor liquido recebido dos pagamentos aprovados. Quando esse valor nao esta disponivel, e derivado dos componentes da venda. Em consultas que filtram apenas alguns itens do pedido, e alocado proporcionalmente." data-tooltip="GMV usa preferencialmente o valor liquido recebido dos pagamentos aprovados. Quando esse valor nao esta disponivel, e derivado dos componentes da venda. Em consultas que filtram apenas alguns itens do pedido, e alocado proporcionalmente."><i class="bi bi-info-circle"></i></span>
+              <span class="fml-help" tabindex="0" aria-label="GMV e o total bruto do pedido: valor dos produtos mais o frete pago pelo comprador, antes de comissoes, cupons, impostos e demais custos. Em consultas que filtram apenas alguns itens do pedido, e alocado proporcionalmente." data-tooltip="GMV e o total bruto do pedido: valor dos produtos mais o frete pago pelo comprador, antes de comissoes, cupons, impostos e demais custos. Em consultas que filtram apenas alguns itens do pedido, e alocado proporcionalmente."><i class="bi bi-info-circle"></i></span>
             </div>
           </div>
         </section>
@@ -197,7 +197,7 @@
           <div class="fml-kpi-label"><i class="bi bi-receipt"></i><span>Base do imposto</span></div>
           <div class="fml-metric-value">
             <strong id="fml-margin-tax-base">R$ 0,00</strong>
-            <span class="fml-help" tabindex="0" aria-label="Soma dos produtos e do frete pago pelo comprador; e independente do GMV de pagamento." data-tooltip="Soma dos produtos e do frete pago pelo comprador; e independente do GMV de pagamento."><i class="bi bi-info-circle"></i></span>
+            <span class="fml-help" tabindex="0" aria-label="Soma dos produtos e do frete pago pelo comprador. Em um pedido completo, esta e a mesma base bruta usada no GMV; a aliquota configurada e aplicada sobre esse valor." data-tooltip="Soma dos produtos e do frete pago pelo comprador. Em um pedido completo, esta e a mesma base bruta usada no GMV; a aliquota configurada e aplicada sobre esse valor."><i class="bi bi-info-circle"></i></span>
           </div>
         </article>
         <article class="fml-kpi">
@@ -350,12 +350,12 @@
                 <th>Pedido</th>
                 <th>Data</th>
                 <th>Modo envio</th>
-                <th><span class="fml-th-label">GMV <span class="fml-help fml-help--table" tabindex="0" aria-label="GMV usa preferencialmente o valor liquido recebido dos pagamentos aprovados. Quando esse valor nao esta disponivel, e derivado dos componentes da venda. Em consultas que filtram apenas alguns itens do pedido, e alocado proporcionalmente." data-tooltip="GMV usa preferencialmente o valor liquido recebido dos pagamentos aprovados. Quando esse valor nao esta disponivel, e derivado dos componentes da venda. Em consultas que filtram apenas alguns itens do pedido, e alocado proporcionalmente."><i class="bi bi-info-circle"></i></span></span></th>
-                <th><span class="fml-th-label">Preço produto <span class="fml-help fml-help--table" tabindex="0" aria-label="Valor dos produtos vendidos no pedido calculado a partir do preco do item no Mercado Livre vezes a quantidade vendida; esta e a antiga base do GMV e e independente do GMV financeiro corrigido." data-tooltip="Valor dos produtos vendidos no pedido calculado a partir do preco do item no Mercado Livre vezes a quantidade vendida; esta e a antiga base do GMV e e independente do GMV financeiro corrigido."><i class="bi bi-info-circle"></i></span></span></th>
+                <th><span class="fml-th-label">GMV <span class="fml-help fml-help--table" tabindex="0" aria-label="GMV e o total bruto do pedido: valor dos produtos mais o frete pago pelo comprador, antes de comissoes, cupons, impostos e demais custos. Em consultas que filtram apenas alguns itens do pedido, e alocado proporcionalmente." data-tooltip="GMV e o total bruto do pedido: valor dos produtos mais o frete pago pelo comprador, antes de comissoes, cupons, impostos e demais custos. Em consultas que filtram apenas alguns itens do pedido, e alocado proporcionalmente."><i class="bi bi-info-circle"></i></span></span></th>
+                <th><span class="fml-th-label">Preço produto <span class="fml-help fml-help--table" tabindex="0" aria-label="Valor dos produtos vendidos no pedido, antes de somar o frete pago pelo comprador. O GMV bruto e formado por este valor mais o frete do comprador." data-tooltip="Valor dos produtos vendidos no pedido, antes de somar o frete pago pelo comprador. O GMV bruto e formado por este valor mais o frete do comprador."><i class="bi bi-info-circle"></i></span></span></th>
                 <th><span class="fml-th-label">CMV <span class="fml-help fml-help--table" tabindex="0" aria-label="Custo dos produtos vendidos, calculado a partir do custo cadastrado para cada SKU e da quantidade vendida." data-tooltip="Custo dos produtos vendidos, calculado a partir do custo cadastrado para cada SKU e da quantidade vendida."><i class="bi bi-info-circle"></i></span></span></th>
                 <th><span class="fml-th-label">Comissoes ML <span class="fml-help fml-help--table" tabindex="0" aria-label="Comissoes liquidas cobradas pelo Mercado Livre neste pedido. O rebate/estorno ja esta considerado neste valor." data-tooltip="Comissoes liquidas cobradas pelo Mercado Livre neste pedido. O rebate/estorno ja esta considerado neste valor."><i class="bi bi-info-circle"></i></span></span></th>
-                <th><span class="fml-th-label">Frete comprador <span class="fml-help fml-help--table" tabindex="0" aria-label="Participa do total financeiro e da Base do imposto." data-tooltip="Participa do total financeiro e da Base do imposto."><i class="bi bi-info-circle"></i></span></span></th>
-                <th><span class="fml-th-label">Base imposto <span class="fml-help fml-help--table" tabindex="0" aria-label="Total tributavel do pedido usado no calculo fiscal: total_amount dos produtos mais o frete pago pelo comprador. Esta base e separada do GMV." data-tooltip="Total tributavel do pedido usado no calculo fiscal: total_amount dos produtos mais o frete pago pelo comprador. Esta base e separada do GMV."><i class="bi bi-info-circle"></i></span></span></th>
+                <th><span class="fml-th-label">Frete comprador <span class="fml-help fml-help--table" tabindex="0" aria-label="Frete pago pelo comprador. Ele e somado ao valor dos produtos para formar o GMV bruto e tambem participa da Base do imposto." data-tooltip="Frete pago pelo comprador. Ele e somado ao valor dos produtos para formar o GMV bruto e tambem participa da Base do imposto."><i class="bi bi-info-circle"></i></span></span></th>
+                <th><span class="fml-th-label">Base imposto <span class="fml-help fml-help--table" tabindex="0" aria-label="Total tributavel do pedido: valor dos produtos mais o frete pago pelo comprador. Em um pedido completo, corresponde a mesma base bruta usada no GMV." data-tooltip="Total tributavel do pedido: valor dos produtos mais o frete pago pelo comprador. Em um pedido completo, corresponde a mesma base bruta usada no GMV."><i class="bi bi-info-circle"></i></span></span></th>
                 <th><span class="fml-th-label">Impostos <span class="fml-help fml-help--table" tabindex="0" aria-label="Imposto do pedido calculado sobre a Base imposto usando a aliquota configurada." data-tooltip="Imposto do pedido calculado sobre a Base imposto usando a aliquota configurada."><i class="bi bi-info-circle"></i></span></span></th>
                 <th><span class="fml-th-label">Tarifa Envios <span class="fml-help fml-help--table" tabindex="0" aria-label="Custo de envio ou logistica cobrado do vendedor e identificado no shipment do Mercado Livre." data-tooltip="Custo de envio ou logistica cobrado do vendedor e identificado no shipment do Mercado Livre."><i class="bi bi-info-circle"></i></span></span></th>
                 <th><span class="fml-th-label">Cupom <span class="fml-help fml-help--table" tabindex="0" aria-label="Parcela de cupom ou desconto efetivamente financiada pelo vendedor neste pedido." data-tooltip="Parcela de cupom ou desconto efetivamente financiada pelo vendedor neste pedido."><i class="bi bi-info-circle"></i></span></span></th>
--- a/tests/financeiro-ml-gmv.test.js
+++ b/tests/financeiro-ml-gmv.test.js
@@ -9,7 +9,6 @@
 const FinanceiroMlService = require("../services/financeiroMlService");
 
 const {
-  resolveOrderPaymentGmv,
   resolveRealizedGmv,
   buildMarginExportWorkbook,
   orderLifecycleStatus,
@@ -103,104 +102,62 @@
   assert.deepEqual(scope.periodRows.map((row) => row.order_id), ["cancelled"]);
 });
 
-test("sums usable payment net received amounts", () => {
-  const result = resolveOrderPaymentGmv({
-    payments: [
-      { id: 11, status: "approved", transaction_details: { net_received_amount: 1200.66 } },
-      { id: 12, status: "approved", net_received_amount: 700 },
-      { id: 13, status: "cancelled", net_received_amount: 900 },
-    ],
+test("uses gross order total plus buyer shipping as GMV", () => {
+  const result = resolveRealizedGmv({
+    orderProductTotal: 734.44,
+    fullOrderRevenue: 734.44,
+    revenueBase: 734.44,
+    buyerShippingFull: 96.71,
+    selectionRatio: 1,
+    totalCosts: 617.04,
   });
 
-  assert.deepEqual(result, {
-    amount: 1900.66,
-    source: "order.payments.total",
-    payment_ids: ["11", "12"],
-  });
+  assert.equal(result.gmv, 831.15);
+  assert.equal(result.source, "order.total_amount+buyer_shipping");
+  assert.equal(result.profit, 214.11);
+  assert.equal(Number(result.marginPct.toFixed(2)), 25.76);
 });
 
-test("counts only approved payments in GMV", () => {
-  const result = resolveOrderPaymentGmv({
-    payments: [
-      { id: "approved-id", status: "approved", net_received_amount: 100 },
-      { id: "in-process-id", status: "in_process", net_received_amount: 80 },
-      { id: "refunded-id", status: "refunded", net_received_amount: 60 },
-    ],
-  });
-
-  assert.deepEqual(result, {
-    amount: 100,
-    source: "order.payments.total",
-    payment_ids: ["approved-id"],
-  });
-});
-
-test("uses paid amount less marketplace fee when net received is unavailable", () => {
-  const result = resolveOrderPaymentGmv({
-    payments: [
-      { id: 21, status: "approved", total_paid_amount: 200, marketplace_fee: 25 },
-    ],
-  });
-
-  assert.deepEqual(result, {
-    amount: 175,
-    source: "order.payments.total",
-    payment_ids: ["21"],
-  });
-});
-
-test("returns unavailable when no usable payments exist", () => {
-  const result = resolveOrderPaymentGmv({
-    payments: [{ id: 31, status: "rejected", net_received_amount: 100 }],
-  });
-
-  assert.deepEqual(result, {
-    amount: null,
-    source: "unavailable",
-    payment_ids: [],
-  });
-});
-
-test("derives realized GMV from the full order when payment totals are unavailable", () => {
+test("does not subtract commissions or coupon while building GMV", () => {
   const result = resolveRealizedGmv({
-    paymentGmv: { amount: null, source: "unavailable", payment_ids: [] },
+    orderProductTotal: 1144.08,
     fullOrderRevenue: 1144.08,
     revenueBase: 1144.08,
     buyerShippingFull: 935.27,
-    fullOrderCommissions: 173.05,
-    sellerCouponDiscountFull: 5.64,
     selectionRatio: 1,
-    totalCosts: 300,
+    totalCosts: 1026.14,
   });
 
-  assert.ok(Math.abs(result.gmv - 1900.66) < 0.000001);
-  assert.equal(result.source, "order.derived.total");
-  assert.deepEqual(result.payment_ids, []);
-  assert.equal(result.product_revenue, 1144.08);
-  assert.ok(Math.abs(result.profit - 1600.66) < 0.000001);
-  assert.ok(Math.abs(result.marginPct - 84.22) < 0.01);
+  assert.equal(result.gmv, 2079.35);
+  assert.equal(result.profit, 1053.21);
 });
 
-test("exports auditable payment-total GMV fields in the period workbook", () => {
+test("exports gross GMV and only STATUS as the added period column", () => {
   const workbook = buildMarginExportWorkbook({
     period_rows: [{
-      order_id: "2000018084427526",
-      gmv: 1900.66,
-      gmv_source: "order.payments.total",
-      payment_ids: ["11", "12"],
-      product_revenue: 1144.08,
-      lifecycle_status: "Concluído",
+      order_id: "2000018284539198",
+      gmv: 831.15,
+      product_revenue: 734.44,
+      product_cost: 435.88,
+      commissions: 73.04,
+      buyer_shipping_paid: 96.71,
+      tax_base: 831.15,
+      taxes: 83.12,
+      coupon_discount: 25,
+      rebate: 22.72,
+      total_costs: 617.04,
+      profit: 214.11,
+      margin_pct: 25.76,
+      lifecycle_status: "Preparando",
     }],
   });
   const sheet = workbook.Sheets["Margem por periodo"];
   const [headers, row] = XLSX.utils.sheet_to_json(sheet, { header: 1 });
 
-  assert.deepEqual(headers.slice(9, 13), [
-    "GMV", "FONTE_GMV", "IDS_PAGAMENTO", "PRECO_PRODUTO",
-  ]);
-  assert.deepEqual(row.slice(9, 13), [
-    1900.66, "order.payments.total", "11 | 12", 1144.08,
-  ]);
+  assert.equal(headers.includes("FONTE_GMV"), false);
+  assert.equal(headers.includes("IDS_PAGAMENTO"), false);
   assert.equal(headers.at(-1), "STATUS");
-  assert.equal(row.at(-1), "Concluído");
+  assert.equal(row[headers.indexOf("GMV")], 831.15);
+  assert.equal(row[headers.indexOf("RESULTADO")], 214.11);
+  assert.equal(row.at(-1), "Preparando");
 });
--- a/tests/financeiro-ml-margem-ui.test.js
+++ b/tests/financeiro-ml-margem-ui.test.js
@@ -32,7 +32,7 @@
   assert.match(periodTable, /Folga\/un\.[\s\S]*?<th>Status<\/th>/);
   assert.match(
     periodTable,
-    /valor dos produtos vendidos no pedido calculado a partir do preco do item no Mercado Livre vezes a quantidade vendida; esta e a antiga base do GMV e e independente do GMV financeiro corrigido/i,
+    /valor dos produtos vendidos no pedido, antes de somar o frete pago pelo comprador\. O GMV bruto e formado por este valor mais o frete do comprador/i,
   );
   assert.match(
     periodTable,
