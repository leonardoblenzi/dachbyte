"use strict";
const {Worker}=require("bullmq");const {ensureRedisConnected}=require("../config/redis");const queueNames=require("../config/queueNames");const accountRepository=require("../repositories/accountRepository");const webhookRepository=require("../repositories/webhookRepository");const {reconcileSku}=require("../services/catalogSyncService");
const SUPPORTED_TOPICS=new Set(["portfolios_sku","portfolios_price","portfolios_stock"]);let worker=null;
function skuFromEvent(event){const direct=String(event?.payload?.data?.params?.sku||"").trim();if(direct)return direct;const resource=String(event?.payload?.data?.resource||"").trim();if(!resource)return"";const path=resource.split("?")[0].replace(/\/+$/,"");const value=path.slice(path.lastIndexOf("/")+1);try{return decodeURIComponent(value);}catch(_){return value;}}
async function processWebhookEvent(eventId){const event=await webhookRepository.claimEvent(eventId);if(!event)return{ignored:true,reason:"event_not_claimable"};try{if(!SUPPORTED_TOPICS.has(event.topic)){await webhookRepository.markEventIgnored(event.id,`unsupported_topic:${event.topic}`);return{ignored:true,reason:"unsupported_topic"};}const account=await accountRepository.findAccountByTenantId(event.magalu_tenant_id);if(!account||account.status!=="active"){await webhookRepository.markEventIgnored(event.id,"account_not_connected");return{ignored:true,reason:"account_not_connected"};}const sku=skuFromEvent(event);if(!sku){await webhookRepository.markEventIgnored(event.id,"sku_missing");return{ignored:true,reason:"sku_missing"};}await webhookRepository.attachEventAccount(event.id,account.id);const result=await reconcileSku(account.id,sku,{topic:event.topic});await webhookRepository.markEventProcessed(event.id);return{ok:true,sku,topic:event.topic,result};}catch(error){await webhookRepository.markEventFailed(event.id,error?.message||String(error)).catch(()=>{});throw error;}}
async function startWebhookProcessWorker(){if(worker)return worker;const connection=await ensureRedisConnected();worker=new Worker(queueNames.webhookProcess,async(job)=>processWebhookEvent(job.data.eventId),{connection,concurrency:3});worker.on("failed",(job,error)=>console.error("[seller-magalu:webhook-worker] job failed",{job:job?.id||null,message:error?.message||String(error)}));return worker;}
async function stopWebhookProcessWorker(){if(!worker)return;const current=worker;worker=null;await current.close();}
module.exports={startWebhookProcessWorker,stopWebhookProcessWorker,_test:{skuFromEvent,SUPPORTED_TOPICS}};
